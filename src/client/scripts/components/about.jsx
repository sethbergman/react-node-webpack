"use strict";

var React = require("react");

var About = React.createClass({
    render: function() {
        return (
            <section>
                <p>This is a demo website built using React, Webpack and Node.js</p>
            </section>
        );
    }
});

module.exports = About;
